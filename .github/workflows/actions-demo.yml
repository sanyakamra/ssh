name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: sanyakamra/ssh@master
      with:
        host: '34.125.156.140'
        username: 'sanya.kamra' 
        key: ${{ secrets.KEY }}
        port: '22'
        script: |
           whoami
           ls


  
