name: CICD
on: push
jobs:
  cicd:
    runs-on: ubuntu-latest
    permissions: { contents: read, packages: write, id-token: write }
    steps:
      - uses: internetarchive/cicd@v1
        with:
          BASE_DOMAIN: 'sanya.kamra@34.125.156.140'
          NOMAD_ADDR: 'http://10.182.0.3'
          NOMAD_TOKEN: ${{ secrets.NOMAD_TOKEN }}
